apiVersion: v1
kind: Service
metadata:
  name: api-python-service
  namespace: ns-scale
spec:
  selector:
    app: api-python
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: ClusterIP
