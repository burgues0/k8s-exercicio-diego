apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-node-ingress
  namespace: ns-dev
spec:
  ingressClassName: traefik
  rules:
    - host: dev.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-node-service
                port:
                  number: 80
