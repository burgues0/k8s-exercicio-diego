apiVersion: v1
kind: Service
metadata:
  name: frontend-node-service
  namespace: ns-dev
spec:
  selector:
    app: frontend-node
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3002
  type: ClusterIP
