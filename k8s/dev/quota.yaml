apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota-dev
  namespace: ns-dev
spec:
  hard:
    pods: "10"
    requests.cpu: "500m"
    requests.memory: "512Mi"
    limits.cpu: "2"
    limits.memory: "2Gi"